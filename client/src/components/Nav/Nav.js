import React, { Component } from 'react'

class Nav extends Component {
  state = {}
  render() { 
    return (
      <nav>
        <button>Sign up</button>
      </nav>
    );
  }
}
 
export default Nav